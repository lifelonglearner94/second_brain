# Backend base URL. Defaults to "/api" (the Edge reverse-proxies /api/* to the
# backend — see infrastructure/CONTEXT.md). Set this only for local dev against
# a backend running on a different origin.
# VITE_BACKEND_BASE_URL=http://127.0.0.1:8080
